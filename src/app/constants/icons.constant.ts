import {
    faAt,
    faLock,
    faUserAlt
} from "@fortawesome/free-solid-svg-icons";

export const Icons = {
    auth: {
        email: faAt,
        password: faLock,
        username: faUserAlt
    }
}